interface:
  display_name: "Grill With Docs"
  short_description: "Stress-test plans against code and docs"
  default_prompt: "Use $grill-with-docs to pressure-test this plan against code, CONTEXT.md, context maps, and ADRs."

policy:
  allow_implicit_invocation: false
