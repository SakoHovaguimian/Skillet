interface:
  display_name: "Code Simplifier"
  short_description: "Simplify recent code without changing behavior"
  default_prompt: "Use $code-simplifier to simplify the recently modified code while preserving behavior and existing architecture."

policy:
  allow_implicit_invocation: false
