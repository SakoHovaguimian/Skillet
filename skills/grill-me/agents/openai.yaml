interface:
  display_name: "Grill Me"
  short_description: "Resolve plans through evidence-backed decisions"
  default_prompt: "Use $grill-me to pressure-test this proposal in dependency-aware rounds of material decisions."

policy:
  allow_implicit_invocation: false
